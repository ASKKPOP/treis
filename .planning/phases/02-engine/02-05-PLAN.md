---
phase: 02-engine
plan: 05
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - packages/core/src/index.ts
  - packages/core/src/plan-contract/index.ts
  - packages/core/src/agent/index.ts
autonomous: true
requirements:
  - PLAN-01
  - PLAN-02
  - PLAN-03
  - PLAN-04
  - PLAN-05
  - PLAN-06
  - PLAN-07
  - PLAN-08
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - AGENT-04
  - AGENT-05
  - AGENT-06
  - AGENT-07
  - AGENT-08

must_haves:
  truths:
    - "All Plan Contract types and functions are importable from @treis/core"
    - "All Agent loop types and functions are importable from @treis/core"
    - "The full @treis/core package builds without errors"
    - "All Phase 2 tests pass in a single test run"
  artifacts:
    - path: "packages/core/src/index.ts"
      provides: "Re-exports all Phase 1 and Phase 2 modules"
      exports: ["PlanContractSchema", "ScopeEntrySchema", "createPlanContractEngine", "checkToolScope", "AgentState", "StateMachine", "CircuitBreaker", "runAgent", "createRetryHandler"]
    - path: "packages/core/src/plan-contract/index.ts"
      provides: "Barrel export for plan-contract module"
      exports: ["PlanContractSchema", "ScopeEntrySchema", "PlanOptionSchema", "createPlanContract", "createPlanContractEngine", "checkToolScope", "ScopeViolation", "ViolationDecision"]
    - path: "packages/core/src/agent/index.ts"
      provides: "Barrel export for agent module"
      exports: ["AgentState", "AgentEvent", "AgentConsumer", "AgentRunOptions", "StateMachine", "CircuitBreaker", "createRetryHandler", "buildRetryInjection", "runAgent", "EXECUTION_LIMITS"]
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/plan-contract/index.ts"
      via: "re-export"
      pattern: "export.*from.*plan-contract"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/agent/index.ts"
      via: "re-export"
      pattern: "export.*from.*agent"
---

<objective>
Wire all Phase 2 modules into the @treis/core public API via barrel exports, then verify the full package builds and all tests pass together.

Purpose: Ensure all Plan Contract and Agent loop types and functions are importable from `@treis/core`, completing D-22. Run a full build + test pass as the phase gate.
Output: Barrel export files for plan-contract and agent modules, updated core index.ts, green build + full test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-engine/02-CONTEXT.md
@.planning/phases/02-engine/02-01-SUMMARY.md
@.planning/phases/02-engine/02-02-SUMMARY.md
@.planning/phases/02-engine/02-03-SUMMARY.md
@.planning/phases/02-engine/02-04-SUMMARY.md

<interfaces>
<!-- All Phase 2 exports to wire -->
```typescript
// packages/core/src/plan-contract/schema.ts
export { PlanContractSchema, ScopeEntrySchema, PlanOptionSchema, PlanOptionsResponseSchema,
         ClarifyResponseSchema, PlanContract, ScopeEntry, PlanOption, PlanOptionsResponse,
         ClarifyResponse, createPlanContract }

// packages/core/src/plan-contract/scope-checker.ts
export { checkToolScope, ScopeViolation, ViolationDecision }

// packages/core/src/plan-contract/engine.ts
export { PlanContractEngine, createPlanContractEngine, PlanContractEngineConfig }

// packages/core/src/agent/types.ts
export { AgentState, StepVerdict, AgentEvent, AgentConsumer, AgentRunOptions, EXECUTION_LIMITS }

// packages/core/src/agent/state-machine.ts
export { StateMachine, TRANSITIONS }

// packages/core/src/agent/circuit-breaker.ts
export { CircuitBreaker, CircuitBreakerResult }

// packages/core/src/agent/retry.ts
export { RetryHandler, createRetryHandler, buildRetryInjection, RetryInjection }

// packages/core/src/agent/executor.ts
export { runAgent }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create barrel exports and update core index</name>
  <files>packages/core/src/plan-contract/index.ts, packages/core/src/agent/index.ts, packages/core/src/index.ts</files>
  <read_first>packages/core/src/index.ts</read_first>
  <action>
    Create `packages/core/src/plan-contract/index.ts`:
    ```typescript
    export {
      PlanContractSchema,
      ScopeEntrySchema,
      PlanOptionSchema,
      PlanOptionsResponseSchema,
      ClarifyResponseSchema,
      createPlanContract,
    } from './schema.js'
    export type {
      PlanContract,
      ScopeEntry,
      PlanOption,
      PlanOptionsResponse,
      ClarifyResponse,
    } from './schema.js'

    export {
      checkToolScope,
    } from './scope-checker.js'
    export type {
      ScopeViolation,
      ViolationDecision,
    } from './scope-checker.js'

    export {
      createPlanContractEngine,
    } from './engine.js'
    export type {
      PlanContractEngine,
      PlanContractEngineConfig,
    } from './engine.js'
    ```

    Create `packages/core/src/agent/index.ts`:
    ```typescript
    export {
      AgentState,
      StepVerdict,
      EXECUTION_LIMITS,
    } from './types.js'
    export type {
      AgentEvent,
      AgentConsumer,
      AgentRunOptions,
    } from './types.js'

    export {
      StateMachine,
      TRANSITIONS,
    } from './state-machine.js'

    export {
      CircuitBreaker,
    } from './circuit-breaker.js'
    export type {
      CircuitBreakerResult,
    } from './circuit-breaker.js'

    export {
      createRetryHandler,
      buildRetryInjection,
    } from './retry.js'
    export type {
      RetryHandler,
      RetryInjection,
    } from './retry.js'

    export { runAgent } from './executor.js'
    ```

    Update `packages/core/src/index.ts` to add Phase 2 exports (keep existing Phase 1 exports):
    ```typescript
    export const VERSION = '0.0.1'

    // Phase 1: Errors + Session
    export * from './errors.js'
    export * from './session/store.js'
    export * from './session/workspace.js'
    export * from './session/persist.js'
    export * from './session/trace-logger.js'
    export * from './session/checkpoint.js'

    // Phase 2: Plan Contract + Agent
    export * from './plan-contract/index.js'
    export * from './agent/index.js'
    ```
  </action>
  <verify>
    <automated>cd /Users/desirey/Programming/src/treis && pnpm --filter @treis/core build</automated>
  </verify>
  <done>All Phase 2 modules are exported from @treis/core. Package builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Full test suite verification</name>
  <files></files>
  <read_first></read_first>
  <action>
    Run the complete test suite for @treis/core to verify all Phase 1 and Phase 2 tests pass together:

    ```bash
    pnpm --filter @treis/core test
    ```

    Then run the full monorepo test suite:

    ```bash
    pnpm test
    ```

    If any tests fail, investigate and fix. Common issues:
    - Import path mismatches (`.js` extension required for ESM)
    - Type export conflicts between barrel files and direct imports
    - Mock cleanup between test files (use `afterEach(() => vi.restoreAllMocks())`)

    After tests pass, verify exports are accessible:
    ```bash
    node -e "
    import('@treis/core').then(m => {
      console.log('PlanContractSchema:', typeof m.PlanContractSchema)
      console.log('AgentState:', m.AgentState)
      console.log('runAgent:', typeof m.runAgent)
      console.log('createPlanContractEngine:', typeof m.createPlanContractEngine)
      console.log('checkToolScope:', typeof m.checkToolScope)
      console.log('StateMachine:', typeof m.StateMachine)
      console.log('CircuitBreaker:', typeof m.CircuitBreaker)
    })
    "
    ```
  </action>
  <verify>
    <automated>cd /Users/desirey/Programming/src/treis && pnpm --filter @treis/core test && pnpm --filter @treis/core build</automated>
  </verify>
  <done>Full @treis/core test suite passes. All Phase 2 exports (plan-contract + agent) are importable from @treis/core. Package builds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Package export surface | Only approved types and functions are exported |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-19 | Information Disclosure | index.ts | mitigate | Only public API types and functions are re-exported; internal helpers remain unexported |
</threat_model>

<verification>
1. `pnpm --filter @treis/core build` — builds without errors
2. `pnpm --filter @treis/core test` — all tests pass (Phase 1 + Phase 2)
3. `pnpm test` — full monorepo test suite passes
4. `grep "plan-contract" packages/core/src/index.ts` — plan-contract module exported
5. `grep "agent" packages/core/src/index.ts` — agent module exported
</verification>

<success_criteria>
- packages/core/src/plan-contract/index.ts barrel exports all plan-contract types and functions
- packages/core/src/agent/index.ts barrel exports all agent types and functions
- packages/core/src/index.ts re-exports both modules alongside Phase 1 exports
- `pnpm --filter @treis/core build` succeeds
- `pnpm --filter @treis/core test` all tests pass
- `pnpm test` full monorepo passes
- All Phase 2 requirement IDs (PLAN-01..08, AGENT-01..08) are covered by tests across plans 01-04
</success_criteria>

<output>
After completion, create `.planning/phases/02-engine/02-05-SUMMARY.md`
</output>
